# openspec/config.yaml
# Hotel Market Pro — Corporate Business Hotel Hub
schema: spec-driven

context: |
  Tech stack: Greenfield — no stack detected yet (empty project dir)
  Architecture: Corporate website / business hotel hub (static/dynamic)
  Testing: No test runner detected — strict_tdd disabled until a runner is configured
  Style: TBD — project is empty, no conventions established

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
    - Consider SEO, performance, and accessibility requirements
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
